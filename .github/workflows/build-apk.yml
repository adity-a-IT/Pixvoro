name: Build and Release Android APK

on:
  push:
    branches:
      - master
      - main
    tags:
      - 'v*'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build-apk:
    name: Build Android APK
    runs-on: ubuntu-latest

    steps:
      - name: ⬇️ Checkout Repository
        uses: actions/checkout@v4

      - name: ☕ Setup Java JDK 17
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '17'

      - name: 🟢 Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: 'npm'

      - name: 📦 Install Dependencies
        run: npm install

      - name: 🔑 Make Gradlew Executable
        run: chmod +x android/gradlew

      - name: 🏗️ Build Android Release APK
        run: |
          cd android
          ./gradlew assembleRelease --no-daemon

      - name: 📋 Find and Prepare APK
        run: |
          mkdir -p build-output
          APK_PATH=$(find android/app/build/outputs/apk/release -name "*.apk" | head -n 1)
          if [ -z "$APK_PATH" ]; then
            APK_PATH=$(find android/app/build/outputs/apk -name "*.apk" | head -n 1)
          fi
          if [ -f "$APK_PATH" ]; then
            cp "$APK_PATH" build-output/Pixvoro.apk
            echo "APK ready at build-output/Pixvoro.apk"
          else
            echo "::error::No APK file found in outputs!"
            exit 1
          fi

      - name: 📤 Upload APK Artifact
        uses: actions/upload-artifact@v4
        with:
          name: Pixvoro-Android-APK
          path: build-output/Pixvoro.apk
          retention-days: 30

      - name: 🚀 Publish GitHub Release
        uses: softprops/action-gh-release@v2
        if: github.ref == 'refs/heads/master' || github.ref == 'refs/heads/main' || startsWith(github.ref, 'refs/tags/')
        with:
          name: Pixvoro Android App (v1.0.0)
          tag_name: v1.0.0
          draft: false
          prerelease: false
          files: build-output/Pixvoro.apk
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
